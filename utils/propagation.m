function sigma = propagation(model,data,sigmaData)
    
    arguments
        model function_handle,
        data (:,:) double {mustBeFinite, mustBeReal},
        sigmaData(:,:) double {mustBeFinite, mustBeReal},
    end
    
end